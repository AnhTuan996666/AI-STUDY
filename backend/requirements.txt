# Dùng ràng buộc tối thiểu (>=) thay vì ghim cứng: Python 3.14 chỉ có sẵn wheel
# ở các bản mới. Khi lên production, chạy `pip freeze > requirements.lock.txt`
# và deploy bằng file lock đó.
fastapi>=0.118
uvicorn[standard]>=0.34
httpx>=0.28
pydantic>=2.12
pydantic-settings>=2.8
email-validator>=2.2

# auth
# psycopg (v3) thay cho asyncpg: có wheel sẵn cho Python 3.14.
pyjwt>=2.10
argon2-cffi>=23.1
sqlalchemy[asyncio]>=2.0
psycopg[binary]>=3.2
alembic>=1.14

# dev
pytest>=8.3
pytest-asyncio>=0.25
ruff>=0.9
taskipy>=1.14
